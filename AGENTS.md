# Repository Instructions

1. `git commit` 必须使用 `type: description` 格式，例如 `fix: handle empty interactor response`。
2. 每次 commit 保持小而聚焦，不要把过多无关改动放进同一个 commit。
3. 当改动已经构成一次合理发布时，应同步更新版本号。
4. 工作完成后，必须将本地提交 push 到远程仓库。
